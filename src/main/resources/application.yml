
server:
  port: 1989

spring:
  redis:
    database: 0
    host: localhost
    port: 6379
    password: coti
    timeout: 60000
  cache:
    type: redis
    cache-names: company
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/redis
    username: root
    password: coti
  jpa:
    generate-ddl: false
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true
  output:
    ansi:
      enabled: Always